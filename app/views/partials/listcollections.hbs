<div class="ui centered cards">
  {{#each collections}}
    <div class="ui card">
      <div class="image">
        <img src="{{picture}}">
      </div>
      <div class="content">
        <a class="header">{{uppercase title}}</a>
        <div class="metadata">
          <span class="date">Created: {{formatDate date}}</span>
        </div>
      </div>
      <div class="extra content">
        <a href="/collection/{{id}}" class="ui icon button">
          <i class="icon folder open"></i>
        </a>
        <a href="/dashboard/deletecollection/{{id}}" class="ui icon button delcollection">
          <i class="icon trash"></i>
        </a>
      </div>
    </div>
  {{/each}}
</div>